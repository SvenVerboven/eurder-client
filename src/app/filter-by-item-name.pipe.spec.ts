import { FilterByItemNamePipe } from './filter-by-item-name.pipe';

describe('FilterByItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
